import React, { ReactElement } from 'react'

interface Props {
    
}

function Loader({}: Props): ReactElement {
    return (
        <div>
            
        </div>
    )
}

export default Loader

