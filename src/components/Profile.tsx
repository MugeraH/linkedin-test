import React, { ReactElement } from 'react'

interface Props {
    
}

function Profile({}: Props): ReactElement {
    return (
        <div>
            
        </div>
    )
}

export default Profile

